# Mousehole v0.5.0 is here!

Hi all,

Another update! Changes for efficiency, consistency, and polish are in store.

## Highlights

- Mousehole now simply **updates MAM on every interval** instead of trying to
  detect when the change is needed. This removes moving parts for simplicity and
  maintains correctness. The old change-detection logic (and
  `MOUSEHOLE_STALE_RESPONSE_SECONDS`) is gone.
- **Breaking**: The `MOUSEHOLE_CHECK_INTERVAL_SECONDS` env var is renamed to
  `MOUSEHOLE_UPDATE_INTERVAL_SECONDS`. This is part of a larger effort to
  make the naming more consistent.
- **Breaking**: The `/ok` endpoint has been removed in favor of `/health`.
- **Breaking**: On the backend, **API endpoints are renamed** for consistency
  and clarity: the endpoints are now `PUT /cookie` (change your cookie value),
  `POST /updates` (trigger an update), and `GET /state` (read the current
  state).
- On the frontend, error handling has been improved.
- The Unraid guide has been overhauled for clarity and the associated template
  updated to match the latest version of Mousehole. Thanks to
  [@chodeus](https://github.com/chodeus).
- The default Dockerfile healthcheck now reports healthy if the service is up
  and serving. This makes it suitable for container orchestration. The body
  however will report the MAM sync state.

## Upgrading

For most Docker users: pull the new image. See the
[changelog](https://github.com/t-mart/mousehole/blob/master/CHANGELOG.md) for
the full list of changes.

_Try it out when you can and give me feedback!_
